using UnityEngine;
using System.Collections;

public class centerBox : MonoBehaviour {

	public Transform Parent;
	// Use this for initialization
	void Start () {
//		Transform centerBox = GetComponent<Transform> ();
//		centerBox.position = Parent.GetComponent<Transform>().position;
//		centerBox.position = new Vector3 (centerBox.position.x,centerBox.position.y + 2f, centerBox.position.z);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
